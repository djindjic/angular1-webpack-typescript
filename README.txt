windows users run this commands with administrator privileges to be sure that you have latest version of windows build tools:
npm install --global --production windows-build-tools

run 'npm install' after every svn update if there was any change in this project

start webpack development server with 'npm start'

run tests with 'npm run test'

build deployment bundle by running 'npm run build'. build version is in 'dist' folder 